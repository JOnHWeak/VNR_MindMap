// js/intro.js
document.addEventListener('DOMContentLoaded', () => {
    // Chờ 3 giây để animation hoàn tất
    setTimeout(() => {
        // Chuyển hướng sang trang mindmap chính
        window.location.href = 'main.html';
    }, 3000); // 3 giây
});
